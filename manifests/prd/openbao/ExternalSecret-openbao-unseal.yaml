apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: openbao-unseal
  namespace: openbao
spec:
  data:
    - remoteRef:
        key: openbao-unseal-key/unseal-key
      secretKey: unsealKey
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: openbao-unseal
