apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: apps-tls
  namespace: gateway
spec:
  dnsNames:
    - '*.kidibox.net'
    - kidibox.net
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: apps-tls
