apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: apps-gateway
  namespace: argocd
spec:
  destination:
    namespace: gateway
    server: https://kubernetes.default.svc
  project: default
  source:
    path: ./manifests/prd/apps-gateway
    repoURL: https://github.com/kid/home-ops.git
    targetRevision: main
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
