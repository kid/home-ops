# Custom RouterOS group definitions, shared across every device (see
# modules/den/aspects/routeros/base.nix for how these get filtered down to
# only the groups each specific device actually needs).
{ }
