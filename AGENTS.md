# AGENTS

- Enter the repo through `direnv`/the flake shell (`use flake .`). `.envrc` sets repo-local `TALOSCONFIG=$(pwd)/talosconfig` and `KUBECONFIG=$(pwd)/kubeconfig`; do not assume global kube or Talos config.
- This is a dendritic flake: `flake.nix` is auto-generated by `flake-file` (`nix run .#write-flake` regenerates it) from modules under `modules/`, imported recursively via `import-tree`. Don't hand-edit `flake.nix` — edit the relevant `modules/**/*.nix` file instead.
- Formatting is treefmt, configured in `modules/flake/formatter.nix`: `nixfmt`, `deadnix`, `statix`, `hclfmt`, `terraform` (for `*.tofu`/`*.tfvars`/`*.tftest.hcl`), `yamlfmt`, and `just`. `*.sops.*` files are excluded. `modules/flake/git-hooks.nix` wires this up via git-hooks-nix: treefmt runs on pre-commit, and the full `nix flake check` (treefmt's flakeCheck + `checks.terragrunt`) runs on pre-push. `.pre-commit-config.yaml` is a generated symlink into the Nix store — do not hand-edit it.

## Repo Shape

- `modules/`: the dendritic Nix module tree. `modules/flake/` is flake plumbing (inputs, devShell, formatter, git-hooks). `modules/den/schema/` and `modules/den/policies/` define den's entity system (`environment`, `network`, `device` entity kinds resolved into the `flake -> fleet -> environment -> {network, device}` scope tree; `user` is a separate, data-only registry — see below). `modules/networks/prd.nix` declares the prd environment's VLANs. `modules/devices/{rb5009,crs320}.nix` declare the RouterOS devices and their per-stack Terraform inputs. `modules/network/aspects/ros-*.nix` and `modules/terragrunt/*.nix` render those into Terragrunt HCL — see "Network / Terragrunt" below. `modules/users/*.nix` declares `den.users.registry` entries (human + service accounts) — see "Users" below.
- `tf-stacks/`: now just `root.hcl` (SOPS-decrypted Cloudflare R2 remote-state config, nothing else) plus the generated `prd/network/**/terragrunt.hcl` leaves. There is no `dev/` stack or `talos`/`talos-dev` stack here anymore — those were removed; Talos/Kubernetes cluster bootstrap lives entirely under `clusters/dev/` (see below), which is unrelated.
- `tf-catalog/`: legacy — the vendored `ros/*` Terraform modules that used to live here were dropped in favor of a separate private `terragrunt-infra-catalog` git repo (see below). Only an empty `_shared` directory remains; don't add new modules here.
- `clusters/dev/`: Kubernetes cluster bootstrap entrypoint. `clusters/dev/kustomization.yaml` applies `./flux` and generates the `cluster-values` ConfigMap from `clusters/dev/cluster.env`.
- `clusters/dev/apps/kustomization.yaml`: the real app-selection overlay for the dev cluster.
- `kubernetes/apps/`: app manifests and Flux `Kustomization`/`HelmRelease` definitions.
- `kubernetes/components/`: shared components used by apps, including `cluster-values`.

## Network / Terragrunt (Nix-generated)

- `tf-stacks/prd/network/**/terragrunt.hcl` are generated files — **never hand-edit them.** Edit the Nix source instead (`modules/devices/rb5009.nix` / `crs320.nix` for per-device inputs, `modules/networks/prd.nix` for VLANs, `modules/network/aspects/ros-*.nix` for the shared per-stack module wiring), then run `nix run .#write-terragrunt` to regenerate. `checks.terragrunt` (part of `nix flake check`) fails if the committed leaves drift from what the Nix modules would generate — this also runs on pre-push.
- VLAN/CIDR data is computed once in Nix (`modules/network/lib.nix`'s `cidrsubnet`/`cidrhost`, mirroring Terraform's functions) and read via `environment.networks.<name>` — not hand-merged per-leaf in HCL anymore.
- Each `ros-*` Terraform module (base/capsman/dns/firewall/qos) is now sourced from a **separate private repo**, `git@github.com:kid/terragrunt-infra-catalog`, pinned per-aspect via `moduleVersion` in `modules/network/aspects/ros-*.nix` (rendered as `terraform.source = "git::...?ref=<module>/v<version>"`). Bumping a module version means editing that aspect file, not `tf-stacks/`.
- The `"terragrunt-stacks"` class and every RouterOS aspect (`ros-*`, each device's own self-aspect) live directly on `den.classes`/`den.aspects` — there used to be a private `den.namespace "tf" false` wrapping all of this (`modules/terragrunt/namespace.nix`, now deleted), but it turned out to be pure export hygiene with zero effect on internal plumbing (confirmed against den's own pinned source and against `nixopslab`'s equivalent `terranix` class, which never used a private namespace either). Don't reintroduce a namespace for this.
- `modules/devices/rb5009.nix` and `crs320.nix` shared several helpers byte-for-byte (`allVlanIds`, `toVlanInput`, `toFirewallVlanInput`, `sharedInputs`) — hoisted into `modules/network/_ros-lib.nix` (leading underscore: `import-tree` skips any path containing `/_`, so this is a plain Nix file, `import`ed directly, not a dendritic module — don't rename it without the underscore or it'll be auto-imported as a broken flake-parts module).
- crs320's own Management-VLAN address/MAC used to be hardcoded a second time in `rb5009.nix` (a DHCP static lease + a firewall rule) — both now read `config.den.devices.crs320.managementHostNum`/`.managementMac` (fields on the device schema, `modules/den/schema/devices.nix`) instead. **A `den.quirks`/`pipe.collect` version of this was tried first and doesn't work**: the custom `"terragrunt-stacks"` class collection in `modules/terragrunt/collect.nix` (its `instantiate` function) reads `builtins.head m.imports` as if it were already-resolved plain data, but requesting a quirk as a named parameter causes den to defer that aspect's content into a wrapped module (only finalized by a real `lib.evalModules` pass, which `instantiate` never performs) — confirmed empirically, not just theoretically. Fixing this for real would mean rewriting `instantiate` to route through `evalModules`, affecting all five `ros-*` stacks. Don't attempt quirks/pipes against this class again without doing that rework first.
- RouterOS auth moved from a SOPS secrets file to 1Password vault/item references (`op_vault`, `op_item_routeros`, etc. in each device's inputs).
- `routeros_groups`/`routeros_users` are **no longer hand-written per device** — `modules/network/aspects/ros-base.nix` derives both from `den.users.registry`/`den.groups` (see "Users" below), filtered to whatever each specific device actually references. Don't add them back to `modules/devices/{rb5009,crs320}.nix`; add/edit registry entries under `modules/users/*.nix` or group definitions in `modules/network/groups.nix` instead.
- **`den.users.registry.kid`'s `devices.{rb5009,crs320}.group = "full"` is still marked `TODO verify`** (ported from the pre-registry placeholder, unconfirmed against the real routers). Do not run `terragrunt apply` against `prd/network/**` until that's confirmed — it would destroy the real users/groups/SSH keys currently on the routers. Per-user passwords still come from 1Password only (Nix never carries them) — every non-`admin` user needs an item titled `"<DEVICE> - user - <username>"` in the `op_vault`.
- `dependencies { paths = [...] }` blocks are still used for `run-all` ordering, computed from each device/stack's `dependsOn` list in `modules/terragrunt/devshell.nix`. Two pre-existing quirks are intentionally preserved rather than silently fixed: rb5009's `qos` stack has no `dependencies` block (unlike its siblings), and crs320's `ether1` has a stray `command = "vulkan"` field (the module only reads `comment`).

## Users

- `den.users.registry` (schema in `modules/den/schema/users.nix`, instances in `modules/users/*.nix`) is a single fleet-wide registry for both human and service accounts — ported from den's own official `templates/fleet-demo` (`github:denful/den`), not an ad-hoc invention. It is deliberately **not** a walkable scope-tree entity (`isEntity` is left unset) the way `device`/`network`/`environment` are.
- NixOS accounts: `fleet.user-access.by-host.<host>.groups` grants a registry user's access-policy `groups` onto a host (`modules/den/policies/users.nix`), which resolves via den's own `resolve.to "user"` into a child `{ host, user }` scope. **Content aspects needing that `{ host, user }` context (`modules/den/aspects/users/{define-user,ssh-keys}.nix`) must be wired via `den.schema.user.includes`, not a host's own `den.aspects.<host>.includes`** — the host's own aspect chain only has `{ host }` bound, so a function requiring `user` silently never fires there. Got this wrong once already; if a registry user's NixOS account renders empty, this is the first thing to check.
- `den.schema.user.classes` needs *some* default (`lib.mkDefault [ ]` is set in `modules/den/schema/users.nix`) — several of den's own built-in batteries (e.g. its unfree-predicate builder) read `user.classes` unconditionally on every resolved user and crash with `attribute 'classes' missing` if it's unset.
- RouterOS side deliberately stays on plain `den.users.registry`/`den.groups` reads in `ros-base.nix` (not `resolve.to`/quirks like the NixOS side) — `den.aspects.ros-base`'s `"terragrunt-stacks"` output is a home-ops-invented class, not den's built-in cross-aspect-assembled NixOS config, so there's no "many decoupled producers" problem to solve; it's one registry read directly by one consumer, same as `den.environments`/`den.networks` elsewhere in this repo (see the "Network / Terragrunt" section above for why quirks/pipes genuinely don't compose with this class).
- RouterOS's built-in `admin` account is a fixed literal in `ros-base.nix` (`admin.disabled = false;`), not a registry entry — it isn't a fleet user and has no `group`.
- `den.groups` (`modules/den/schema/groups.nix`, instances in `modules/network/groups.nix`) only needs entries for *custom* RouterOS groups (`external-dns`, `metrics`); built-ins (`full`, `write`, `read`) are referenced by name with no entry required.

## Flux And Kubernetes

- Do not edit `kubernetes/apps/kustomization.yaml` to add apps for the dev cluster. It is intentionally empty; cluster composition happens in `clusters/dev/apps/kustomization.yaml`.
- `clusters/dev/flux/ks.apps.yaml` injects `postBuild.substituteFrom` for `ConfigMap/cluster-values` and optional `Secret/namespace-secrets` into most Flux `Kustomization` objects. Do not duplicate that wiring unless you need to opt out.
- Preserve `${...}` placeholders in manifests. Many values are resolved by Flux post-build substitution from `clusters/dev/cluster.env`, not by shell expansion.
- Preserve Flux ordering fields such as `dependsOn`, `wait`, and namespaces.
- Use `clusters/dev/cluster.env` as the source of truth for cluster-wide values like domains, CIDRs, and storage class names.

## PR Deployment

- For temporary live testing, keep `Kustomization/flux-system` reconciling. The branch override lives in `ConfigMap/flux-system/cluster-branch-override` as `data.CLUSTER_BRANCH` and is consumed by `clusters/dev/flux/gitrepository.yaml` through `Kustomization/flux-system.spec.postBuild.substituteFrom`.
- When working from a branch, always open a PR for it before live testing.
- Before deploying a PR, check whether `ConfigMap/flux-system/cluster-branch-override` already exists with a non-default `CLUSTER_BRANCH`. Do not replace an active PR deployment without explicit user approval.
- Create or patch `ConfigMap/flux-system/cluster-branch-override` with `data.CLUSTER_BRANCH=<branch>`; that override will be consumed by `Kustomization/flux-system` and reapply `GitRepository/flux-system` with the same branch value from git. Do not use GitHub PR merge refs here; this cluster's Flux/source-controller version may resolve them without fetching the commit object.
- Reconcile the owning app `Kustomization` to apply changed git-managed manifests from the fetched branch. Reconcile a `HelmRelease` only when you need to re-run Helm after the `HelmRelease` spec has already been applied.
- Keep the override `ConfigMap` in place while the cluster should stay on the PR branch ref; once `Kustomization/flux-system` reconciles from git it will continue to enforce the overridden branch until you update or remove `ConfigMap/flux-system/cluster-branch-override`.
- For rollback or cleanup, prefer waiting until the PR is merged so the normal branch contains the tested changes before switching the cluster back.
- After merge, deleting `ConfigMap/flux-system/cluster-branch-override` should be sufficient to restore the normal source branch; `clusters/dev/flux/gitrepository.yaml` defaults to `main` when no override is present.

## Talos

- The dev cluster in `clusters/dev/talos/config.yaml` is three control-plane nodes (`nodes/talos{0,1,2}.yaml`); there are no dedicated workers.
- `clusters/dev/talos/patches/schedule-on-controlplane.yaml` enables scheduling on control planes. Do not assume workloads must avoid them.
- `clusters/dev/talos/patches/cilium.yaml` sets Talos CNI to `none`; cluster networking is expected to come later from Kubernetes manifests.

## Verification

- `nix flake check` is the main gate: `checks.treefmt` (formatting), `checks.check-flake-file` (generated `flake.nix` matches `modules/flake/inputs.nix` etc.), and `checks.terragrunt` (generated leaves match committed ones). It also runs automatically on pre-push via git-hooks-nix.
- This sandbox typically has no sops/age decryption keys or network access to the real RouterOS devices, so `terragrunt plan`/`apply` against `tf-stacks/prd/network/**` can't be verified here — treat any change there as needing a real zero-diff plan check by the user before applying.
- `.github/workflows/dagger-checks.yaml` and `.github/workflows/flux-diff.yaml` still invoke `dagger call flux-local ...`, but the `dagger/` directory (including `dagger/flux-local`) and `dagger.json` were removed from this repo — those CI workflows are currently dangling and will fail until they're either removed or the flux-local checks are reimplemented some other way. There is currently no working local equivalent for validating `kubernetes/`/`clusters/` changes before pushing.
