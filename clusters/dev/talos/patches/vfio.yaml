machine:
  kernel:
    modules:
      - name: vfio
      - name: vfio_pci
        parameters:
          - ids=1000:00e6
      - name: vfio_iommu_type1
  # files:
  #   - path: /etc/modprobe.d/vfio-sas.conf
  #     op: create
  #     permissions: 0o644
  #     content: |
  #       options vfio-pci ids=1000:00e6
  #       blacklist mpt3sas
