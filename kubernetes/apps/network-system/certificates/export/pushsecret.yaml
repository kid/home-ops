---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: &name letsencrypt-tls
spec:
  secretStoreRefs:
    - name: ${ONEPASSWORD_STORE}
      kind: ClusterSecretStore
  selector:
    secret:
      name: *name
  template:
    data:
      tls.crt: '{{ index . "tls.crt" | b64enc }}'
      tls.key: '{{ index . "tls.key" | b64enc }}'
  data:
    - match:
        secretKey: &crt tls.crt
        remoteRef:
          remoteKey: ${APP_DOMAIN} - tls
          property: *crt
    - match:
        secretKey: &key tls.key
        remoteRef:
          remoteKey: ${APP_DOMAIN} - tls
          property: *key
