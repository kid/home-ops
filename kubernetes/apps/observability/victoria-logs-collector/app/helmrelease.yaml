---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: victoria-logs-collector
spec:
  chartRef:
    kind: OCIRepository
    name: victoria-logs-collector
  interval: 1h
  targetNamespace: observability
  values:
    remoteWrite:
      - url: &vlogsUrl http://victoria-logs-vl-server.observability.svc.cluster.local:9428
        maxDiskUsagePerURL: 10GB
    fullnameOverride: vl-collector
    extraArgs:
      tmpDataPath: /var/lib/vl-collector
    collector:
      excludeFilter: kubernetes.pod_name:=%{HOSTNAME}
      includePodLabels: true
      includePodAnnotations: false
      includeNodeLabels: false
      includeNodeAnnotations: false
      # vlagent can't rename dynamic kubernetes.* metadata into OpenTelemetry k8s.* fields,
      # but it can attach static OTel-style fields that downstream queries expect.
      extraFields: >-
        {"log.source":"kubernetes"}
    tolerations:
      - key: node-role.kubernetes.io/control-plane
        operator: Exists
        effect: NoSchedule
      - key: node-role.kubernetes.io/master
        operator: Exists
        effect: NoSchedule
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        memory: 512Mi
