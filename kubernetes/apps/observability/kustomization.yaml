---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

components:
  - ../../components/alerts

transformers:
  - ./namespace-transformer.yaml

resources:
  - ./namespace.yaml
  - ./kube-prometheus-stack/ks.app.yaml
